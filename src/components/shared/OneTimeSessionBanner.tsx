/**
 * OneTimeSessionBanner - DEPRECATED
 * 
 * The OT session timer is now integrated directly into ProfileIndicator.tsx
 * This component is kept for backwards compatibility but renders nothing.
 * 
 * @deprecated Use ProfileIndicator instead, which now shows OT session time
 */
const OneTimeSessionBanner = () => {
  // Timer OT ora integrato in ProfileIndicator
  return null;
};

export default OneTimeSessionBanner;
